extends layout

block content
  h1= title

  ul
    each client in client_list
      li
        a(href=client.url) #{client.name}
        |  (#{client.domain})

    else
      li There are no clients.
